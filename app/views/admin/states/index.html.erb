<h1>All states</h1>

<div class="states">
  <% @states.each do |state| %>
  <div class="state">
    <%= link_to state.name, admin_state_path(state) %>
  </div>
  <% end %>
</div>

<div class="btn">
  <%= link_to "Add State", new_admin_state_path %>
</div>
